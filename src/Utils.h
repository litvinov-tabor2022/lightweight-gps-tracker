#ifndef LIGHTWEIGHT_GPS_TRACKER_UTILS_H
#define LIGHTWEIGHT_GPS_TRACKER_UTILS_H

#include "Arduino.h"

class Utils {
public:
    static String randomString(int len);

    static char randomChar();
};


#endif //LIGHTWEIGHT_GPS_TRACKER_UTILS_H
